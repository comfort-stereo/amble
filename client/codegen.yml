overwrite: true
schema: "../common/generated/schema.graphql"
documents:
  - "**/*.{graphql,ts,tsx}"
  - "!(node_modules/**/*)"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "fragment-matcher"
      - "typescript"
      - "typescript-operations"
hooks:
  afterAllFileWrite:
    - "prettier --write"
